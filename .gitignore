# Python Gitignore File
# This file helps Git ignore specific files and directories that shouldn't be tracked in version control

# Byte-compiled / optimized / DLL files
# Python creates these files to improve performance, but they don't need to be version controlled
__pycache__/
*.py[cod]
*$py.class

# C extensions
# Compiled C library files
*.so

# Distribution / packaging
# Files and directories related to Python package distribution
.Python
env/                    # Virtual environment directories
venv/
ENV/
*.egg-info/             # Package metadata
_build/
dist/                   # Distribution and build directories
build/
eggs/
parts/
wheels/
share/bbb-pkg/
*.whl
*.egg
sdist/

# PyInstaller
# Executable files and build artifacts (usually not needed in version control)
# Uncomment if you want to include these
# build/
# dist/
# /onefile

# IDE-specific project settings
.spyderproject           # Spyder IDE settings
.spyproject
.ropeproject             # Rope refactoring library settings

# Environment and configuration files
client_secret*
token.json
.env                    # Environment variable files
.venv/                  # Explicitly ignore .venv directory
.venv
venv/
env/
venv/
ENV/
*/.venv/                # Virtual environments in subdirectories
*/.env/

# Jupyter Notebook
.ipynb_checkpoints      # Jupyter notebook temporary files

# Python version management
# Uncomment if you want to track Python version
# .python-version

# Package management logs and caches
pip-log.txt             # Pip installation logs
pip-delete-this-directory.txt
pip-download-cache/

# Dependency management
poetry.lock             # Poetry dependency lock file

# Distribution uploads
dist/                   # Distribution files for PyPI

# Celery (distributed task queue) schedule
celerybeat-schedule

# Documentation builds
docs/_build/            # Sphinx documentation build directory

# Temporary and swap files
*.swp                   # Vim swap files
*.tmp                   # Temporary files
tmp/                    # Temporary directory

# Project-specific ignores
# Ignore all hidden folders (those starting with a dot)
# This prevents tracking of system or hidden configuration folders
.**/

# Ignore specific project folders
# Useful for keeping old or deprecated code, temporary storage, or system-specific folders out of version control
.RooCodeMemoryZip/     # Ignore RooCodeMemory temporary storage folder
archive/               # Ignore archive folder with old or deprecated code

# Ignore specific files that shouldn't be tracked
# For example, global workspace rules that might contain sensitive or local-specific configurations
ws_global_rules.md
